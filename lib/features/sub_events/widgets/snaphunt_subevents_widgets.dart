import 'package:flutter/material.dart';

class SnapHuntSubEvents extends StatelessWidget {
  const SnapHuntSubEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.amber,
      body: Center(
        child: Text("Snaphunt Subevents"),
      ),
    );
  }
}
